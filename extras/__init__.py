"""LMC-5 extras — opt-in reference implementations.

These packages are NOT imported by `src/lmc5`. They are reference
implementations of production-shaped backends (pgvector, real
embedders, real LLM rerankers) plus the hook layer that wires
LMC-5 into agent runtimes.

Install dependencies à la carte; see each subpackage README.
"""
